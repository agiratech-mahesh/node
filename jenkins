pipeline {
    agent any 
    stages {
        stage('Verify') {
            steps {
                sh '''
                    docker version
                    docker-compose version
                '''
            }
        }
            stage('build') {
                steps {
                    sh ' npm install'
                }
            }
            stage('docker compose') {
                steps {
                    sh 'docker-compose up -d '
                }

            }
        }
    }

